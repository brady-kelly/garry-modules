# Testing Your Code

This project now contains unit tests to check the code your will be writing. To get these up and running, enter the command:
```
pip install pytest
```

Then use the following command to run all your tests:
